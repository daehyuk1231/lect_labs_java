<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app.labs.dept.dao.DeptRepository">

	<select id="getDeptCount" parameterType="int" resultType="int">
		select count(*) from dept
		<if test="deptNo != null">
			where deptno=#{deptNo} <!-- 파라메터명 기준 -->
		</if>
	</select>

	<select id="getDeptList" resultType="app.labs.dept.model.Dept">
		select * from dept
	</select>

	<select id="getDeptInfo" parameterType="int"
		resultType="app.labs.dept.model.Dept">
		SELECT deptno, dname, loc
		FROM dept
		WHERE deptno =
		#{deptNo}
	</select>

	<insert id="insertDept" parameterType="app.labs.dept.model.Dept">
		insert into dept
		(deptno,
		dname, loc)
		values
		(#{deptNo},
		#{dName}, #{loc})
	</insert>

	<update id="updateDept" parameterType="app.labs.dept.model.Dept">
		update dept
		set
		dname=#{dName}, loc=#{loc}
		where deptno=#{deptNo}
	</update>

	<delete id="deleteDept">
		delete from dept
		where deptno=#{deptNo}
	</delete>

</mapper>